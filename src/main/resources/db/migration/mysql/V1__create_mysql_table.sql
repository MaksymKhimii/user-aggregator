CREATE TABLE IF NOT EXISTS user_table (
                            ldap_id VARCHAR(255) PRIMARY KEY,
                            ldap_login VARCHAR(255),
                            name VARCHAR(255),
                            surname VARCHAR(255)
);
